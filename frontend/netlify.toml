# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
[build.environment]
  NODE_VERSION = "22"
# React Router SPA redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
