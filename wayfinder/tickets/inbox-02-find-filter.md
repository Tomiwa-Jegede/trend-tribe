## Question
Inbox/Chat currently has no find/filter loop — just flat list. Should we add a find (search by name/title) loop hole audit: does filtering need debounced search, empty state, and should it stay out of scope for this map vs add as future ticket?
