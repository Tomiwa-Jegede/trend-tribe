# mkt-exec-02-listing-badge — CLOSED

## Question
Implement Verified Student Badge on `ListingCard` + `ListingDetailPage` + `ProfilePage`: navy pill `✓ Verified [school] · ...[matric last-4]` from `backend/prisma/schema.prisma:28-32`, mask full matric, show fresher `schema.prisma:29` where applicable.

## Type
wayfinder:task

## Notes
- Implements [mkt-03](mkt-03-trust-badge.md), consult `ui-ux-pro-max`
- Blocked by: mkt-exec-01-home-hero (hero trust strip visual should match)


## Resolution
Built locally `3b0069b` — code in `frontend/` + `public/posters/`, `npm run build` ok, not pushed.
